<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Search results</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <div class="header-wrap">
        <h1>Mini Message Board</h1>
        <%- include('partials/navbar', {links: links}) %>
      </div>
    </header>

    <main id="main-wrapper">
      <h2>Search results</h2>

      <!-- <form action="/search-result" method="GET" class="search-form">
        <label for="name">Name: </label>
        <input type="text" id="name" name="name" />
        <br />
        <br />
        <button type="submit">Search message</button>
      </form> -->

      <p>Search result for: <%= name %></p>
      <% if(messages.length < 1) { %>
      <p>Sorry no messages found!</p>
      <% } %>
      <ul>
        <% for (let i = 0; i < messages.length; i++) { %>
        <li>
          <div class="search-result__wrapper">
            <span><b><%= messages[i].username %></b></span>
            <span> <%= messages[i].msg_cont %> </span>
            <a href="message/<%= messages[i].id %>"> View details </a>
          </div>
        </li>
        <% } %>
      </ul>
      <a href="/search">Search again</a>
    </main>

    <footer>
      <h3>Glorious Footer</h3>
    </footer>
  </body>
</html>
